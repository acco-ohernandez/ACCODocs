<Window x:Class="ACCODocs.Forms.AddUserLinkWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Link"
        Width="380" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Title" Margin="0,0,0,2"/>
        <TextBox Grid.Row="1" x:Name="TxtTitle" Padding="4,3" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="2" Text="URL" Margin="0,0,0,2"/>
        <TextBox Grid.Row="3" x:Name="TxtUrl" Padding="4,3" Margin="0,0,0,12"
                 Text="https://"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Add" Width="80" Margin="0,0,8,0" IsDefault="True" Click="BtnAdd_Click"/>
            <Button Content="Cancel" Width="80" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
